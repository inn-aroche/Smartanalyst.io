# @smartanalyst/web (placeholder)

App SaaS (front authentifié). À démarrer une fois l’API stable et les premiers parcours UX décidés.

Pistes envisagées :
- Next.js (App Router) si on veut du SSR/ISR partiel.
- Vite + React si on reste pur SPA côté client (l’API porte tout).

**Décision repoussée** — pas de code ici tant qu’on n’a pas tranché. Le dossier existe pour réserver l’emplacement dans le workspace.
