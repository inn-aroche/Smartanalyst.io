---
import DarkBase from '../../../layouts/DarkBase.astro';
import Mockup from '../../../components/Mockup.astro';
import LogoStrip from '../../../components/LogoStrip.astro';
import '../../../styles/subpage.css';
import { PRODUCT_FEATURES } from '../../../data/product-features.js';

// Page-level visual extras keyed by slug.
const CONNECTOR_LIVE = [
  { icon: 'ga4' },
  { icon: 'meta' },
  { icon: 'google_ads' },
  { icon: 'stripe' },
  { icon: 'search_console' },
];
const CONNECTOR_ROADMAP = [
  { icon: 'shopify' },
  { icon: 'hubspot' },
  { icon: 'mailchimp' },
  { icon: 'linkedin' },
  { icon: 'klaviyo' },
  { icon: 'notion' },
];

export async function getStaticPaths() {
  return PRODUCT_FEATURES.map((feature) => ({
    params: { slug: feature.slug },
    props: { feature },
  }));
}

const { feature } = Astro.props;
const t = feature.fr;

// Related features: 3 random others.
const related = PRODUCT_FEATURES.filter((f) => f.slug !== feature.slug).slice(0, 3);

const breadcrumbJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Accueil', item: 'https://smartanalyst.io/fr' },
    { '@type': 'ListItem', position: 2, name: 'Produit', item: 'https://smartanalyst.io/fr/product' },
    { '@type': 'ListItem', position: 3, name: t.kicker, item: `https://smartanalyst.io/fr/product/${feature.slug}` },
  ],
};
---
<DarkBase title={t.title.replace(/\.$/, '')} description={t.hero} locale="fr" jsonLd={[breadcrumbJsonLd]}>
  <section class="sp-hero">
    <div class="sp-hero-glow"></div>
    <div class="sp-hero-inner">
      <span class="sp-kicker">{t.kicker}</span>
      <h1 class="sp-title">{t.title}</h1>
      <p class="sp-tagline">{t.tagline}</p>
      <p class="sp-hero-lead">{t.hero}</p>
      <Mockup slug={feature.slug} locale="fr" />
    </div>
  </section>

  {feature.slug === 'connectors' && (
    <section class="sp-section">
      <div class="sp-section-inner">
        <h2 class="sp-h2">5 sources branchées en 1 clic chacune</h2>
        <LogoStrip label="Live" items={CONNECTOR_LIVE} align="left" />
        <div style="height:24px"></div>
        <LogoStrip label="Roadmap" items={CONNECTOR_ROADMAP} align="left" />
      </div>
    </section>
  )}

  <section class="sp-section">
    <div class="sp-section-inner">
      <h2 class="sp-h2">{t.h2_problem}</h2>
      <p class="sp-prose">{t.problem}</p>
    </div>
  </section>

  <section class="sp-section">
    <div class="sp-section-inner">
      <h2 class="sp-h2">{t.h2_how}</h2>
      <div class="sp-steps">
        {t.how.map((step) => (
          <div class="sp-step">
            <h3 class="sp-step-title">{step.title}</h3>
            <p class="sp-step-body">{step.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="sp-section">
    <div class="sp-section-inner">
      <h2 class="sp-h2">{t.h2_examples}</h2>
      <ul class="sp-examples">
        {t.examples.map((ex) => <li>{ex}</li>)}
      </ul>
    </div>
  </section>

  <section class="sp-section">
    <div class="sp-section-inner">
      <h2 class="sp-h2">{t.h2_under_hood}</h2>
      <p class="sp-prose">{t.under_hood}</p>
    </div>
  </section>

  <section class="sp-cta">
    <div class="sp-hero-glow" style="top:-40px;opacity:0.6"></div>
    <div class="sp-cta-inner">
      <h2>{t.cta_title}</h2>
      <p>{t.cta_body}</p>
      <a href="/fr/beta" class="btn btn-primary">Démarrer l’essai gratuit</a>
    </div>
  </section>

  <section class="sp-related">
    <div class="sp-section-inner">
      <h2 class="sp-h2">Autres features</h2>
      <div class="sp-related-grid">
        {related.map((f) => (
          <a href={`/fr/product/${f.slug}/`} class="sp-related-card">
            <div class="sp-related-icon">{f.icon}</div>
            <h3 class="sp-related-title">{f.fr.kicker}</h3>
            <p class="sp-related-tagline">{f.fr.tagline}</p>
          </a>
        ))}
      </div>
    </div>
  </section>
</DarkBase>
